namespace ce;

public class FileDataService
{
    public List<T> Read<T>(string filePath)
    {
         /* Logic to read and deserialize JSON file to a List<T> */
         return null;
    }

    public void Write<T>(string filePath, List<T> data)
    {
         /* Logic to serialize List<T> and write to JSON file */
    }
}